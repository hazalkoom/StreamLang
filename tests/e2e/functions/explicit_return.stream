// EXPECT: 10
function check(n: Int) -> Int {
    if n < 0 {
        return -1;
    } else {
        if n == 0 {
            return 0;
        }
    }
    // This is the implicit return if no early exit happens
    n
}

print(check(10));